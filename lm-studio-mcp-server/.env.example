# LM Studio MCP Server Configuration

# LM Studio endpoint (will auto-discover if not set)
LM_STUDIO_ENDPOINT=http://10.171.217.128:1234

# Default model to use (leave empty to use first available)
DEFAULT_MODEL=

# Maximum context tokens for requests
MAX_CONTEXT_TOKENS=4096

# Connection retry settings
RECONNECT_ATTEMPTS=5
RECONNECT_DELAY=1000

# Logging level
LOG_LEVEL=info

# Auto-discovery settings
DISCOVERY_ENABLED=true
DISCOVERY_INTERVAL=30000